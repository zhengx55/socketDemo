import React from "react";
import styled from "styled-components";
import { AppContainer } from "../Match";

function Battle() {
  return <AppContainer bg="url(/img/bg.png)"></AppContainer>;
}

export default React.memo(Battle);
